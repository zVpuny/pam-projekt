<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projektpam">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjektPam">
        <activity
            android:name=".about.AboutActivity"
            android:exported="false" />
        <activity
            android:name=".shoppinglist.ShoppingListAddEntriesActivity"
            android:exported="false" />
        <activity
            android:name=".unitcalc.UnitCalcActivity"
            android:exported="false" />
        <activity
            android:name=".timer.TimerActivity"
            android:exported="false" />
        <activity
            android:name=".shoppinglist.ShoppingListActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".MediaPlayerService" />
    </application>

</manifest>